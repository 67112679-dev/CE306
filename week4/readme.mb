อธิบายการทำงานของแต่ละส่วน
1. โหลดข้อมูลผู้ใช้ (loadUsers)

ใช้ fs.readFileSync เพื่ออ่านไฟล์ users.json

แปลงข้อมูล JSON เป็น Object ด้วย JSON.parse

ถ้าอ่านไฟล์ไม่ได้ จะคืนค่าเป็น [] และแสดง error

2. แสดงทักษะ (renderSkillTag)

รับข้อมูลทักษะ (skill) เช่น { name: "React", level: "Intermediate" }

ถ้ามี level จะคืนค่าเป็น "React (Intermediate)"

ถ้าไม่มี level จะคืนค่าเป็น "React"

3. แสดงโปรไฟล์ผู้ใช้ (renderUserProfileCard)

แสดงข้อมูลผู้ใช้ทีละคน เช่น

ชื่อ (Name)

อีเมล (Email)

สถานะออนไลน์/ออฟไลน์

ทักษะทั้งหมด (เรียกใช้ renderSkillTag)

แสดงข้อความจำลองปุ่ม "👉 [ดูรายละเอียด]"

4. ฟังก์ชันดูรายละเอียด (onViewDetails)

รับ userId มาเป็นพารามิเตอร์

แสดงข้อความว่า "🔎 ดูรายละเอียดผู้ใช้ ID: <id>"

5. ฟังก์ชันหลัก (App)

โหลดข้อมูลผู้ใช้ทั้งหมดด้วย loadUsers()

ถ้าไม่มีข้อมูล → แสดง "❌ ไม่มีข้อมูลผู้ใช้"

ถ้ามีข้อมูล → loop แสดงการ์ดของผู้ใช้แต่ละคน (renderUserProfileCard)

หลังจากแสดงแต่ละการ์ด จะเรียก onViewDetails(user.id) เพื่อจำลองว่ากดปุ่มดูรายละเอียด

6. users.json

เป็นไฟล์เก็บข้อมูลผู้ใช้ในรูปแบบ JSON

แต่ละ user จะมี:

id (รหัสผู้ใช้)

name (ชื่อ)

email (อีเมล)

isOnline (สถานะออนไลน์/ออฟไลน์)

skills (อาร์เรย์เก็บทักษะ)

7. วิธีทำงาน

รันคำสั่ง node app.js

โปรแกรมจะอ่าน users.json

แสดงข้อมูลผู้ใช้แต่ละคนออกมาบน console

หลังจากนั้นจะแสดงข้อความว่า "ดูรายละเอียด" ของแต่ละ user